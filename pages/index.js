export default function Home() {
  return (
    <main style={{fontFamily:'sans-serif', padding:20}}>
      <h1>Hover Intake is running</h1>
      <p><a href="/api/health">/api/health</a> • <code>/api/intake</code> (POST)</p>
    </main>
  );
}
